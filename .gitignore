# Private working copies. The tracked architecture-overview.html always
# carries generic sample data; save your own drawing to a `-mine.html`
# copy in the same folder and it stays out of git.
*-mine.html
.local-*.html

# macOS metadata
.DS_Store

# Editor / tooling directories
.claude/
.vscode/
.idea/

# Verification harness devDependency (npm install) — not shipped to users
node_modules/
